import React from "react"

import styled from 'styled-components'
import { theme } from "components/common/styled"


const Wrapper = styled.div`

`

export const CartInfoBlock: React.FC= () => {
    
    return(
        <Wrapper>

        </Wrapper>
    )
}